# Normalise all text to LF in the repo. build.js inlines content.js + app.js
# into the index.html shell to generate marginal-preview.html; CRLF drift would
# make that generated file differ across machines. Keep everything LF.
* text=auto eol=lf
