{
  "name": "marginal",
  "private": true,
  "description": "Marginal \u2014 HSC Economics study app (dev tooling only; the app itself is dependency-free static files).",
  "scripts": {
    "lint:css": "stylelint index.html",
    "test": "node build.js && node tests/run.js",
    "gate:fast": "node tests/gate.js fast",
    "gate:checkpoint": "node tests/gate.js checkpoint",
    "gate:journeys": "node tests/gate.js journeys",
    "gate:full": "node tests/gate.js full",
    "gate:snapshot": "node tests/snapshot.js"
  },
  "devDependencies": {
    "postcss-html": "^1.7.0",
    "stylelint": "^16.10.0",
    "playwright": "^1.47.0"
  }
}
